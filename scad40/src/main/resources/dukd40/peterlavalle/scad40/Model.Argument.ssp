#import(peterlavalle.scad40.Model)
<%@ val modelArgument: Model.Argument %>
<%@ val nephrite: peterlavalle.scad40.Nephrite %>
#if (modelArgument.kind == Model.KindString || modelArgument.kind.isInstanceOf[Model.KindDeclaration])
const ${nephrite(modelArgument.kind).trim}& ${modelArgument.name}
#else
${nephrite(modelArgument.kind).trim} ${modelArgument.name}
#end