#import(peterlavalle.scad40.Model)
<%@ val modelMemberFunction: Model.MemberFunction %>
<%@ val nephrite: peterlavalle.scad40.Nephrite %>
${nephrite(modelMemberFunction.resultKind).trim} ${modelMemberFunction.name} (${modelMemberFunction.arguments.foldLeft("")((l, n) => l + ", " + nephrite(n).trim).replaceAll("^, ", "").trim});
